# 개발·문서 작업 흐름

## 1. 정본과 갱신 위치

| 변경한 정보 | 갱신할 문서 |
|---|---|
| 목표·제품 동작·핵심 화면·범위 | [제품 명세](01_PRODUCT_SPEC.md) |
| 구조·데이터·색 분석·저장 계약 | [기술 명세](02_TECH_SPEC.md) |
| 진행·작업·문제·검증·인수인계 | [프로젝트 현황](03_PROJECT_STATUS.md) |
| 작업·검증·문서 운영 절차 | 이 문서 |
| AI의 읽기·갱신 경로 | [AGENTS.md](AGENTS.md) |
| 장기적으로 남길 중요한 새 결정의 이유 | 필요할 때 `adr/ADR-xxx.md` |

원본 핸드오프는 결정 배경과 시각 레퍼런스로 보존한다. 같은 제품 사실을 여러 정본에서 중복 관리하지 않는다.

## 2. 기획과 구현 시작

1. 제품 명세의 목표·제외 범위·관련 `FR-xxx`와 `BR-xxx`·완료 조건을 확인한다.
2. 현황의 현재 작업, 미정 사항, 검증 공백을 확인한다.
3. 기존 코드·패키지·라우팅·사용자 변경을 확인하고 필요한 작업 범위를 정한다.
4. **앱 코드 작성 전에 [Expo SDK 57 버전 문서](https://docs.expo.dev/versions/v57.0.0/)를 읽는다.** 루트 [AGENTS.md](../AGENTS.md)의 필수 지침이다. 패키지 사용법을 과거 SDK 기준으로 추정하지 않는다.
5. 데이터 저장·좌표 변환·이미지 분석 등 여러 계층을 연결하는 구현은 필요한 경우 짧은 계획을 작성한 뒤 진행한다. 문서 정리만으로 기술 실험까지 완료했다고 간주하지 않는다.

## 3. 구현 원칙

- 기존 Expo Router·TypeScript 구조와 설치된 라이브러리를 우선 재사용한다. 필요 없는 상태관리·서버·아키텍처를 추가하지 않는다.
- 작은 목적 단위로 구현한다. 기능의 실제 호출 흐름과 관련 코드를 확인하고 문제의 원인을 고친다.
- 한 사진의 다중 색, 사용자 최종 선택, Local-first, 촬영 후 분석, Color Index 경험을 유지한다.
- 기존 사용자 파일과 변경사항을 임의로 되돌리지 않는다. `reset-project`는 `src`·`scripts`를 이동하거나 삭제할 수 있으므로 일상 작업 시작 명령으로 사용하지 않는다.
- 독립적인 작업은 필요할 때 하위 에이전트로 나누고 수정 범위를 겹치지 않게 정한다. 현재 도구에서 지원하는 모델·추론 옵션을 사용하며 메인 작업자가 통합과 최종 검증을 책임진다.
- 제품 동작 변경은 제품 명세, 데이터·구조 변경은 기술 명세를 같은 작업에서 갱신한다. 실행 상태는 현황에서만 관리한다.
- 명세와 구현이 충돌하면 `DOC-CODE-MISMATCH`, 근거가 부족한 결정은 `NEEDS-DECISION`으로 현황에 영향과 다음 확인을 남긴다. 미구현 기능과 잘못 구현된 기능은 구분한다.

### 역할별 모델 정책

2026-09-07 사용자 지정 기본값이다. 더 적합한 구성이 있으면 작업 난이도·실패 비용·호출 가능 상태·전체 비용과 완료 시간을 근거로 조정할 수 있다. 변경 이유와 실제 사용한 구성을 짧게 알린다.

| 모델 | 추론 수준 | 기본 역할 |
|---|---|---|
| GPT-6 Astra | `high` | 문제 구조화, 설계, 엄격한 검증. 요구 누락·설계 모순·실패 경로를 검토하고 완료 근거를 확인한다. |
| GPT-5.6 Sol | `high` | 메타 프롬프트 작성, 작업 순서·의존성·진행 관리. 위임할 목표·문맥·수정 범위·완료 조건을 구체화한다. |
| GPT-5.6 Sol | `xhigh` | 실제 구현 담당 배정과 복잡한 구현. Astra의 설계를 실행 가능한 변경으로 연결하고 구현 결과를 통합한다. |
| GPT-5.6 Luna | `max` | 범위와 설계가 명확한 일반 구현 및 해당 변경에 필요한 검증. |

- 여러 계층에 걸친 작업은 구조화·설계 → 작업 순서와 위임 프롬프트 → 구현 배정·병렬 구현 → 통합·엄격한 검증으로 진행한다. 독립 작업만 병렬화하고 파일 수정 범위를 분리한다. 메인 에이전트가 전체 완료를 책임진다.
- 작은 작업은 역할을 합쳐 직접 처리할 수 있다. 매 작업마다 네 역할을 모두 생성할 필요는 없다. 단순 반복 작업에서 Luna `max`의 비용이 과하면 더 낮은 추론 수준을 추천·적용할 수 있다.
- 모델 이름은 선호 정책이다. 실제 호출에는 현재 도구가 지원하는 정확한 모델 ID와 추론 옵션을 확인한다. 목록 등록과 실제 호출 성공을 구분하며 확인 목적의 시험 호출을 매번 수행하지 않는다.
- 지정 모델을 호출할 수 없으면 가용한 모델로 역할을 대체하고 이유를 알린다. 인증·사용량 실패는 같은 요청을 반복하지 않고 우회하며, 가용 상태가 회복되면 원래 모델을 다시 후보에 포함한다. 도구가 지원하지 않는 모델 전환을 수행했다고 보고하지 않는다.
- 엄격한 검증은 반복 횟수가 아니라 실제 결함을 찾는 근거로 판단한다. 변경에 관련된 경계값·권한·저장 중간 실패·동시 입력·데이터 손실과 성능 위험을 확인하고 미실행 항목을 구분한다. 검증 통과 후 새 변경이나 미해결 우려가 없으면 완료한다.

### Orca 구현 오케스트레이션

2026-09-07 사용자 지시에 따라 실제 앱 구현은 Orca CLI의 별도 워크트리·세션으로 분담한다. 런타임과 버전에 맞는 `orca-cli`, `orchestration` 가이드를 읽고 Run → Task → Dispatch를 생성한다. 코디네이터는 의존성·파일 소유 범위를 나누며 결과를 통합하고 검증한다. 일반 하위 에이전트 실행을 Orca 작업으로 표시하지 않는다. 구현 전 기획·디자인 체크포인트를 먼저 커밋·푸시한다. 구현 결과의 추가 push·PR·merge는 별도 승인 범위를 따른다.

## 4. 검증

변경 규모에 맞는 검증을 실행하고 결과·날짜·대상 리비전·출처·미실행 공백을 현황에 남긴다. 통과 후 새 변경이나 미해결 우려가 없다면 검사를 반복 확대하지 않는다.

| 변경 | 필요한 확인 |
|---|---|
| 문서만 변경 | 원본 결정·수치·예외 보존, 링크/경로, 요구사항 ID 참조, 현재 코드와 상태 표기, diff |
| UI·라우팅 | TypeScript 검사, 영향받는 화면·상태 확인, 필요한 lint, 가독성·터치·접근성 |
| 좌표·색 변환·추천 | 경계 좌표·회전/표시 비율, 알려진 색 입력과 추천 순서, 다중 선택 및 분석 실패를 잡는 최소 실행 검사 |
| 파일·SQLite 저장 | 재실행 복원, 사진-샘플 연결, 반복 저장·중간 실패·파일 누락, 데이터 손실 방지 |
| 핵심 흐름·출시 | 실기기에서 촬영 → 다중 색 선택 → 저장 → Book, 권한 거부·재시도·오프라인·성능 |

현재 명령과 준비 상태는 기술 명세와 프로젝트 현황을 따른다. 테스트 러너·ESLint 설정·빌드 구성이 없는 상태를 검사 통과로 처리하지 않는다. 정적 검사만으로 카메라·픽셀 추출·영구 저장을 검증했다고 쓰지 않는다.

## 5. 작업 종료와 인수인계

`변경 → 필요한 검증 → 완료 조건 확인 → diff/영향 범위 확인 → 작업 상태·다음 작업·인수인계 갱신`

- [ ] 요청 범위를 처리하고 관련 없는 변경·사용자 변경의 유실이 없다.
- [ ] 실제 실행한 검사와 실패·미실행 이유를 기록했다.
- [ ] 변경한 제품·기술 계약과 실행 상태가 문서에 반영됐다.
- [ ] 현황에 주요 변경 파일, 미완료 사항, 바로 할 일, 보존할 제약을 남겼다.
- [ ] 재시도 방지에 필요한 실패 접근이 있다면 이유를 남겼다.

## 6. 계획과 결정 문서

로드맵·작업·인수인계는 현황 한 파일에 둔다. 단순 문구·UI 수정에는 별도 계획을 만들지 않는다.

여러 계층 변경, DB 스키마·마이그레이션, 복잡한 기능처럼 설계가 유용할 때만 `plans/PLAN-xxx.md`를 만든다. 최소 항목은 ID, 제목, 상태(`DRAFT / READY / IN_PROGRESS / DONE / CANCELLED`), 관련 FR/BR, 목표·포함/제외 범위, 현재 전제, 단계, 변경 파일·데이터 영향, 검증, 미정 사항, 완료 조건이다.

중요한 새 결정은 `adr/ADR-xxx.md`에 상태(`제안 / 승인 / 대체됨`), 날짜, 배경, 결정, 영향을 기록한다. 승인되지 않은 설계를 승인 상태로 쓰지 않는다. 빈 양식·빈 자산 폴더를 구조 맞추기용으로 만들지 않는다.

## 7. Git·데이터 보호

- 커밋·push·PR·merge·원격 변경은 사용자가 명시적으로 요청한 범위에서 수행한다.
- 커밋 전에 적절한 검증과 staged diff를 확인한다. 관련 없는 변경·비밀정보를 포함하지 않는다.
- 커밋 메시지와 PR 제목·본문은 한국어로 작성한다. 제목 형식은 `<분류>: <변경 내용>`이며 분류는 `기능 / 수정 / 문서 / 테스트 / 리팩터링 / 설정 / 제거`다.
- 하나의 목적에 필요한 코드·검증·문서를 묶고, 실행하지 않은 검사를 통과로 적지 않는다.
- 사진·개인 기록·인증정보를 외부로 전송하거나 로그·문서에 불필요하게 남기지 않는다.

## 8. 이번 문서 이관의 경계

기존 핸드오프의 정보 위치는 [README](README.md)에, 현재 검증 공백은 현황에 기록한다. 원본 파일은 보존하며 문서 이관을 이유로 앱 구현·의존성 설치·빌드·실험을 수행하지 않는다. 이후 구현에서는 해당 작업에 필요한 검증을 별도로 실행한다.
